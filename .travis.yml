language: python

python: 
 - "3.7"

install:
 - pip3 install -r requirements.txt



script:
 - python -m unittest discover
 - behave
 - coverage erase
 - coverage run test_app.py && coverage html

after_succes:
 - codecov

